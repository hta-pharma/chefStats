% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/by_strata_by_trt.R
\name{demographics_counts}
\alias{demographics_counts}
\title{Calculate basic summary statistics for demographics}
\usage{
demographics_counts(
  dat,
  cell_index,
  subjectid_var,
  stratify_by,
  strata_var,
  ...
)
}
\arguments{
\item{dat}{data.table. The analysis data set.}

\item{cell_index}{A vector of integers referencing the rows of \code{dat} (as
specified by the \code{INDEX_} column in \code{dat}) that match the population to be
analyzed. See the "Endpoint Events" vignette in \pkg{ramnog}
for more information.}

\item{subjectid_var}{character. Name of the subject identifier variable in the data (default is "USUBJID").}

\item{stratify_by}{character vector. Set of variables in the analysis data to stratify by.}

\item{strata_var}{character. Variable in the analysis data to stratify by specific for this call.}

\item{...}{Optional parameters.}
}
\value{
A data.table containing the summary statistics.
}
\description{
Calculate summary statistics (n_non_missing, n_missing) on a variable for demographics endpoints.
}
\examples{
dat <- data.table::data.table(
  USUBJID = c("S1", "S2", "S3", "S4"),
  SEX     = c("M", "F", "M", NA_character_)
)
dat[, INDEX_ := .I]
data.table::setkey(dat, INDEX_)
demographics_counts(dat, cell_index = dat[["INDEX_"]],
                    subjectid_var = "USUBJID",
                    stratify_by = c("TOTAL_", "SEX"),
                    strata_var = "SEX")
}
